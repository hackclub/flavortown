<style>
.seller-portal__title { font-size: 1.5rem; margin: 0 0 0.25rem; }
.seller-portal__subtitle { color: #6b7280; margin: 0 0 1.5rem; }

.seller-portal__stats { display: flex; gap: 1rem; margin-bottom: 1.5rem; }
.seller-portal__stat {
  background: var(--color-tan-300, #faf7f2);
  border: 1px solid var(--color-brown-500, #8b7355);
  border-radius: 0.5rem;
  padding: 1rem 1.5rem;
  text-align: center;
  flex: 1;
}
.seller-portal__stat-value { display: block; font-size: 1.75rem; font-weight: 700; }
.seller-portal__stat-label { font-size: 0.8rem; color: #6b7280; }

.seller-portal__filters { display: flex; flex-wrap: wrap; gap: 0.75rem; margin-bottom: 1.5rem; }
.seller-portal__filter-group { display: flex; gap: 0.25rem; }
.seller-portal__filter-btn {
  padding: 0.35rem 0.75rem;
  border-radius: 6px;
  font-size: 0.85rem;
  text-decoration: none;
  color: inherit;
  background: var(--color-tan-300, #faf7f2);
  border: 1px solid var(--color-brown-500, #8b7355);
}
.seller-portal__filter-btn--active {
  background: var(--color-brown-700, #5a4a3a);
  color: white;
  border-color: var(--color-brown-700, #5a4a3a);
}

.seller-portal__muted { color: #6b7280; }
.seller-portal__order-meta { display: flex; flex-wrap: wrap; gap: 1.5rem; align-items: center; }
.seller-portal__label { font-size: 0.75rem; color: #6b7280; display: block; }
.seller-portal__qty { font-size: 1.25rem; font-weight: 700; }
.seller-portal__qty--warn { color: #dc2626; }

.seller-portal__address-reveal { padding: 1rem; background: #f3f4f6; border-radius: 4px; text-align: center; }
.seller-portal__address-reveal p { margin: 0 0 1rem; color: #374151; }

.seller-portal__fulfill-form { display: flex; flex-direction: column; gap: 0.75rem; max-width: 400px; }
.seller-portal__fulfill-form label { display: flex; flex-direction: column; gap: 0.25rem; font-size: 0.875rem; font-weight: 500; }
.seller-portal__actions { margin-top: 1.5rem; }

.badge { padding: 0.25rem 0.5rem; border-radius: 4px; font-size: 0.875rem; font-weight: 500; text-transform: capitalize; display: inline-block; }
.badge-pending { background-color: #fbbf24; color: #78350f; }
.badge-awaiting_periodical_fulfillment { background-color: #60a5fa; color: #1e3a8a; }
.badge-awaiting_verification { background-color: #f97316; color: white; }
.badge-fulfilled { background-color: #10b981; color: white; }
.badge-rejected { background-color: #ef4444; color: white; }
</style>
