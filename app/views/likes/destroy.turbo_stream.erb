<%
  likeable_type = @likeable.class.name.underscore.gsub("/", "_")
%>
<%= turbo_stream.replace "like_button_#{likeable_type}_#{@likeable.id}" do %>
  <%= render "likes/button", likeable: @likeable %>
<% end %>
