<div class="prize">
  <div class="image-wrapper">
    <%= image_tag(
      prize[:image_url],
      alt: prize[:name] || "Prize",
      loading: "eager",
      decoding: "async",
      srcset: prize[:srcset],
      sizes: prize[:srcset].present? ? "(max-width: 800px) 144px, 224px" : nil
    ) if prize[:image_url] %>
  </div>

  <div class="description">
    <div class="name"><%= prize[:name] %></div>
    <%= prize[:hours_estimated] >= 500 ?
          ">500 hours" :
          "~#{prize[:hours_estimated]} #{"hour".pluralize(prize[:hours_estimated])}" %>
  </div>
</div>
