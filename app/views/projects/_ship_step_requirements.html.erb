<div class="projects-ship__step">
  <h2 class="projects-ship__section-title">Shipped Project Requirements</h2>
  <p class="projects-ship__section-desc">
    Every project submitted must be fully "shipped." Use the checklists below to confirm your project qualifies.
  </p>

  <div class="projects-ship__validations">
    <% @project.shipping_validations.each do |validation| %>
      <div class="projects-ship__validation <%= 'projects-ship__validation--passed' if validation[:passed] %>">
        <span class="projects-ship__validation-icon">
          <% if validation[:passed] %>
            <%= inline_svg_tag("icons/check_thick.svg") %>
          <% end %>
        </span>
        <span class="projects-ship__validation-label"><%= validation[:label] %></span>
      </div>
    <% end %>
  </div>
</div>
