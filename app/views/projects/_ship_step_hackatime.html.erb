<div class="projects-ship__step">
    <h2 class="projects-ship__section-title">What's Being Shipped</h2>
  <p class="projects-ship__section-desc">
    <% if @last_ship %>
      These devlogs will be included in this ship (since your last ship on <%= @last_ship.created_at.strftime("%B %d, %Y") %>).
    <% else %>
      These devlogs will be included in your first ship.
    <% end %>
  </p>

  <% if @devlogs_for_ship.any? %>
    <div class="projects-ship__posts">
      <%= render PostComponent.with_collection(@devlogs_for_ship, current_user: current_user, compact: true) %>
    </div>
  <% else %>
    <div class="projects-ship__empty-state">
      <p>No new devlogs since your last ship. Add a devlog to document your progress!</p>
    </div>
  <% end %>
</div>
