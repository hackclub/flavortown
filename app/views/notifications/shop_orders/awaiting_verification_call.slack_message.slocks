header "📞 Order ##{order.id} Requires a Verification Call"

section "Your order is being reviewed and requires a short verification call before it can be fulfilled.", markdown: true

section "Order Details:",
  fields: [
    mrkdwn_text("*Order ID:* #{order.id}"),
    mrkdwn_text("*Item:* #{order.shop_item.name}"),
    mrkdwn_text("*Quantity:* #{order.quantity}"),
    mrkdwn_text("*Total:* 🍪 #{order.total_cost}"),
  ]

divider

context [
  mrkdwn_text("We'll reach out to schedule a quick call. Thank you for your patience!")
]
