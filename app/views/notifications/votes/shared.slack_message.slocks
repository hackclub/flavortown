header "🗳️ New Vote Submitted"

section "A vote was submitted for *#{project.title}*", markdown: true

if reason.present?
  divider

  sanitized_reason = reason.gsub(/<@[A-Z0-9]+>/, '[mention]').gsub(/@(here|channel|everyone)/, '[@\\1]')
  section sanitized_reason
end

context [
  mrkdwn_text("Voted by: #{anonymous ? 'Anonymous' : "<@#{voter_slack_id}>"}")
]
