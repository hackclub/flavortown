<div class="shop-order">
    <div class="shop-order__back">
        <%= render ButtonComponent.new(
            text: "← Back to the Shop",
            href: "/shop",
            color: :brown,
            variant: :borderless
        ) %>
    </div>
    <div class="shop-order__content">
        <div class="shop-order__view"
             data-controller="customs-warning"
             data-customs-warning-item-type-value="<%= customs_warning_item_type(@shop_item) %>"
             data-customs-warning-us-origin-message-value="⚠️ Shipped from US - Customs fees may apply if shipping outside the US"
             data-customs-warning-unknown-origin-message-value="⚠️ Origin unknown - Customs fees may apply depending on destination country"
             data-action="address-select:change@document->customs-warning#addressChanged">
            <div class="shop-order__image">
                <%= image_tag @shop_item.image, loading: "lazy", decoding: "async" if @shop_item.image.attached? %>
            </div>
            <h2><%= @shop_item.name %></h2>
            <div class="shop-order__description"><%= md(@shop_item.description) %></div>

            <% if is_warehouse_item?(@shop_item) && @shop_item.agh_contents.present? %>
                <div class="shop-order__agh-contents">
                    <h4>Package Contents:</h4>
                    <ul>
                        <% @shop_item.agh_contents.each do |item| %>
                            <li><%= item["quantity"] %>x <%= item["sku"] %></li>
                        <% end %>
                    </ul>
                </div>
            <% end %>

            <div class="shop-order__customs-warning" data-customs-warning-target="warning" style="display: none;"></div>
        </div>
        <div class="shop-order__details" data-controller="order-form" data-order-form-has-addresses-value="<%= current_user.addresses.any? %>">
            <h2>Complete your order:</h2>
            <%= form_with url: shop_order_path(shop_item_id: @shop_item.id), method: :post, local: true, id: "order-form" do |form| %>
                <% if not @shop_item.one_per_person_ever %>
                    <div class="shop-order__quantity">
                        <%= render InputComponent.new(
                        label: "Quantity",
                        placeholder: 1,
                        form: form,
                        attribute: :quantity,
                        subtitle: "Maximum #{@shop_item.max_qty || 10} items per order",
                        color: :red,
                        as: :number_field,
                        input_html: { min: 1, max: 10, step: 1, value: 1, id: "shop-order__quantity-input" }
                        ) %>
                    </div>
                <% else %>
                    <%= form.hidden_field :quantity, value: 1 %>
                <% end %>

                <%
                  tagged_accessories = @accessories.select(&:has_tag?).group_by(&:accessory_tag)
                  untagged_accessories = @accessories.reject(&:has_tag?)
                %>

                <% if tagged_accessories.any? %>
                    <div class="shop-order__accessories">

                         <% tagged_accessories.each do |tag, accessories_in_group| %>
                             <div class="shop-order__accessory-group-section">
                                 <h5><%= tag.titleize.gsub('_', ' ') %></h5>
                                 <div class="shop-order__accessory-options">
                                     <% accessories_in_group.each do |accessory| %>
                                         <label class="shop-order__accessory-option-label">
                                             <input type="radio"
                                                    name="accessory_tag_<%= tag %>"
                                                    value="<%= accessory.id %>"
                                                    class="shop-order__accessory-option-input"
                                                    data-price="<%= accessory.ticket_cost %>"
                                                    data-tag="<%= tag %>"
                                                    form="order-form"
                                                    style="display: none;">
                                             <div class="shop-order__accessory-option-block">
                                                 <div class="shop-order__accessory-option-name"><%= accessory.name %></div>
                                                 <div class="shop-order__accessory-option-price">🍪 <%= accessory.ticket_cost %></div>
                                             </div>
                                         </label>
                                     <% end %>
                                 </div>
                             </div>
                         <% end %>

                        <% if untagged_accessories.any? %>
                            <div class="shop-order__other-upgrades">
                                <h5>Other upgrades</h5>
                                <div class="shop-order__accessory-group">
                                    <% untagged_accessories.each do |accessory| %>
                                        <div class="shop-order__accessory-item">
                                            <% if accessory.image.attached? %>
                                                <%= image_tag accessory.image.variant(:carousel_sm), class: "shop-order__accessory-image", loading: "lazy" %>
                                            <% end %>
                                            <label class="shop-order__accessory-label">
                                                <input type="checkbox"
                                                    name="accessory_ids[]"
                                                    value="<%= accessory.id %>"
                                                    class="shop-order__accessory-checkbox"
                                                    data-price="<%= accessory.ticket_cost %>">
                                                <span class="shop-order__accessory-name"><%= accessory.name %></span>
                                            </label>
                                            <span class="shop-order__accessory-price">🍪 <%= accessory.ticket_cost %></span>
                                        </div>
                                    <% end %>
                                </div>
                            </div>
                        <% end %>
                    </div>
                <% end %>
            <% end %>
            <% user_addresses = current_user.addresses %>
            <div class="shop-order__address" data-controller="address-select" data-addresses="<%= user_addresses.to_json %>">
                <% if user_addresses.any? %>
                    <div class="shop-order__address-details">
                        <%
                          primary_addr = user_addresses.find { |a| a["primary"] } || user_addresses.first
                          address_options = user_addresses.map do |addr|
                            {
                              label: "#{addr["first_name"]} #{addr["last_name"]} - #{addr["line_1"]}, #{addr["city"]}",
                              value: addr["id"]
                            }
                          end
                          longest_option = address_options.max_by { |o| o[:label].length }&.dig(:label) || ""
                        %>
                        <input type="hidden" name="address_id" form="order-form" id="address-id-input" value="<%= primary_addr["id"] %>" data-address-select-target="input">
                        <div data-address-select-target="dropdown">
                            <%= render DropdownComponent.new(
                                label: "Shipping Address",
                                options: address_options,
                                selected_option: "#{primary_addr["first_name"]} #{primary_addr["last_name"]} - #{primary_addr["line_1"]}, #{primary_addr["city"]}",
                                longest_option_string: longest_option,
                                color: :blue
                            ) %>
                        </div>

                    </div>
                    <div class="shop-order__edit-address">
                        <a href="<%= HCAService.address_portal_url(return_to: request.original_url) %>" class="btn btn-order" style="margin-top: 0.5em;">Edit addresses</a>
                    </div>
                <% else %>
                    <div class="shop-order__no-address">
                         <p>No shipping address found.</p>
                         <a href="<%= HCAService.address_portal_url(return_to: request.original_url) %>" class="btn btn-order">Add an address</a>
                    </div>
                <% end %>
            </div>
            <div class="shop-order__summary">
                <%= render InputGroupComponent.new(
                    label: "Order Summary",
                    color: :green,
                    subtitle: "Current Balance: 🍪#{ current_user.balance }",
                ) do %>
                    <div class="shop-order__summary-content">
                        <div class="shop-order__summary-price">
                            <h4>Base Price:</h4>
                            <h4>🍪 <%= @shop_item.ticket_cost %></h4>
                        </div>
                        <div class="shop-order__summary-accessories-list" id="shop-order__summary-accessories-list" style="display: none;">
                            <h4>Accessories:</h4>
                            <ul id="shop-order__accessories-list-items"></ul>
                        </div>
                        <div class="shop-order__summary-qty">
                            <h4>Quantity:</h4>
                            <h4 id="shop-order__summary-qty-display">1x</h4>
                        </div>
                        <div class="shop-order__summary-total">
                            <h4>Total:</h4>
                            <h4 id="shop-order__summary-total-display">🍪 <%= @shop_item.ticket_cost %></h4>
                        </div>
                    </div>
                <% end %>
            </div>
            <button type="submit" form="order-form" class="btn btn-order" style="width: 100%;" data-order-form-target="submitButton">
                <%= inline_svg_tag "icons/tick.svg", class: "shop-order__summary-btn-icon", aria: { hidden: true } %>
                Get!
            </button>
        </div>
    </div>
</div>
<script>
document.addEventListener('DOMContentLoaded', function() {
    const quantityInput = document.getElementById('shop-order__quantity-input');
    const summaryQtyDisplay = document.getElementById('shop-order__summary-qty-display');
    const summaryTotalDisplay = document.getElementById('shop-order__summary-total-display');
    const accessoriesListContainer = document.getElementById('shop-order__summary-accessories-list');
    const accessoriesListItems = document.getElementById('shop-order__accessories-list-items');
    const accessoryCheckboxes = document.querySelectorAll('.shop-order__accessory-checkbox');
    const accessoryRadios = document.querySelectorAll('.shop-order__accessory-option-input');
    const submitButton = document.querySelector('[data-order-form-target="submitButton"]');
    const baseTicketCost = <%= raw (@shop_item.ticket_cost || 0).to_json %>;
    const userBalance = <%= raw (current_user.balance || 0).to_json %>;
    const initialGetButtonHTML = submitButton?.innerHTML || '';

    function getSelectedAccessories() {
        const selected = [];
        accessoryCheckboxes.forEach(function(checkbox) {
            if (checkbox.checked) {
                const label = checkbox.closest('label');
                const name = label.querySelector('.shop-order__accessory-name').textContent;
                const price = parseFloat(checkbox.dataset.price) || 0;
                selected.push({ name: name, price: price });
            }
        });
        accessoryRadios.forEach(function(radio) {
            if (radio.checked) {
                const optionBlock = radio.closest('label').querySelector('.shop-order__accessory-option-block');
                const name = optionBlock.querySelector('.shop-order__accessory-option-name').textContent;
                const price = parseFloat(radio.dataset.price) || 0;
                selected.push({ name: name, price: price });
            }
        });
        return selected;
    }

    function calculateAccessoriesTotal() {
        let total = 0;
        getSelectedAccessories().forEach(function(acc) {
            total += acc.price;
        });
        return total;
    }

    function updateOrderSummary() {
         const qty = parseInt(quantityInput ? quantityInput.value : 1) || 1;
         const selectedAccessories = getSelectedAccessories();
         const accessoriesTotal = selectedAccessories.reduce((sum, acc) => sum + acc.price, 0);
         const itemTotal = baseTicketCost * qty;
         const grandTotal = itemTotal + accessoriesTotal;

         // Update accessories list
         if (accessoriesListContainer && accessoriesListItems) {
             if (selectedAccessories.length > 0) {
                 accessoriesListContainer.style.display = 'block';
                 accessoriesListItems.innerHTML = selectedAccessories.map(function(acc) {
                     return '<li>' + acc.name + ' <span>🍪 ' + acc.price.toFixed(2) + '</span></li>';
                 }).join('');
             } else {
                 accessoriesListContainer.style.display = 'none';
                 accessoriesListItems.innerHTML = '';
             }
         }

         if (summaryQtyDisplay) {
             summaryQtyDisplay.textContent = qty + 'x';
         }

         if (summaryTotalDisplay) {
             summaryTotalDisplay.textContent = '🍪 ' + grandTotal.toFixed(2);
         }

         if (submitButton) {
             const canAfford = userBalance >= grandTotal;
             const shortfall = Math.max(0, grandTotal - userBalance);

             if (canAfford) {
                 submitButton.disabled = false;
                 submitButton.innerHTML = initialGetButtonHTML;
             } else {
                 submitButton.disabled = true;
                 submitButton.innerHTML = 'You need 🍪 ' + shortfall.toFixed(0) + ' more cookies!';
             }
         }
     }

    if (quantityInput) {
         quantityInput.addEventListener('input', updateOrderSummary);
     }

     accessoryCheckboxes.forEach(function(checkbox) {
         checkbox.addEventListener('change', updateOrderSummary);
     });

     accessoryRadios.forEach(function(radio) {
         radio.addEventListener('change', updateOrderSummary);
     });

     updateOrderSummary();

    const addressSelect = document.getElementById('address-select');
    const addressPreview = document.getElementById('address-preview');
    const addresses = <%= raw (current_user.addresses || []).to_json %>;

    if (addressSelect && addressPreview) {
        addressSelect.addEventListener('change', function() {
            const selectedId = this.value;
            const addr = addresses.find(a => a.id === selectedId);
            if (addr) {
                let html = '<p>' +
                    addr.first_name + ' ' + addr.last_name + '<br>' +
                    addr.line_1;
                if (addr.line_2) {
                    html += '<br>' + addr.line_2;
                }
                html += '<br>' + addr.city + ', ' + addr.state + ' ' + addr.postal_code +
                    '<br>' + addr.country + '</p>';
                addressPreview.innerHTML = html;
            }
        });
    }
});
</script>
