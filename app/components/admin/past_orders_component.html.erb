<div class="admin-section">
  <% if collapsible %>
    <details style="cursor: pointer;">
      <summary class="admin-section__summary">
        📋 <%= title %> (<%= orders.count %>)
      </summary>
      <div class="admin-section__content">
        <%= render "admin/past_orders_component/orders_table", orders: orders %>
      </div>
    </details>
  <% else %>
    <h2 class="admin-section__header">
      📋 <%= title %> (<%= orders.count %>)
    </h2>
    <%= render "admin/past_orders_component/orders_table", orders: orders %>
  <% end %>
</div>
